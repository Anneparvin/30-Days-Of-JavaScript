// ✅ reverse() Method in JavaScript
// The .reverse() method:

// Reverses the order of the elements in the array.

// Changes the original array (it is a destructive method).

// Returns the same array, just reversed.




const reverseArry = [1,2,3,4,5]
reverseArry.reverse()
console.log(reverseArry);


reverseArry.reverse()
console.log(reverseArry);
// .reverse() just flips the array. Use it once to reverse, and again to bring it back.